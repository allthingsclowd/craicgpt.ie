[pytest]
# Only collect the real suite under tests/. The root-level test_*.py files are
# stale Lambda-era scripts (they sys.exit on import) — not part of the pipeline.
testpaths = tests
addopts = -q
